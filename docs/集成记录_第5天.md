# 集成记录 — 第5天(成员D)

> 日期:2026-09-03 · 执行:成员D · 目标:**合并 A+C 的最小可玩版本,记录问题**
> 仓库:github.com/Kun27906/SanDaoPoker (main 分支)

## 一、本次集成做了什么

把 A(核心逻辑)、B(AI)、C(渲染)、D(素材/构建)当前 main 上的全部代码**合并编译 + 逐项验证**,产出问题清单。没有新写功能,专注"能不能跑通、缺什么"。

## 二、验证结果(全部实测)

| 验证项 | 结果 | 证据 |
|--------|------|------|
| 全量编译(主程序+3个工具+测试) | ✅ 47/47 | `cmake --build build` 通过 |
| A 单元测试(牌型/235吃豹子/双王/A23) | ✅ 26 通过 0 失败 | `test_hand_evaluator.exe` |
| B AI 行为检验(多样性+风格对战500局) | ✅ 正常 | `human_test.exe`(见下) |
| 游戏启动(主菜单) | ✅ 运行正常 | 含 BGM/音效加载无报错 |
| 组牌界面(选道/放牌/交牌/倒计时/超时自动交) | ✅ 功能完整 | SceneArrange 代码走查 |
| A 的 Room/Round 逻辑接入界面 | ✅ 已接线 | 组牌数据走 `room->players[].putCard()` 等 |

**AI 检验输出摘录**:
```
理性贪心:1 种方案(每次最优)  |  加噪声人性化:5 种方案  |  纯随机:200 种
风格对战(500局):均衡 vs 激进 49.8%:50.2% ≈ 五五开; 激进 vs 稳健 56.2%:43.0%
```

## 三、问题清单(集成发现,按优先级)

### 🔴 高(影响"完整一局"闭环)
1. **SceneBattle(比牌界面)是骨架** —— 只有标题+提示,无四家牌面、无逐道翻牌、无胜负判定展示。
   计划归属:C 第6天任务(★SceneBattle)。
2. **SceneResult(结算界面)是骨架** —— 无筹码变化、无总账、无"继续/退出"。
   计划归属:C 第7天任务(SceneResult)。

### 🟡 中
3. **组牌界面的 AI 对手是"假随机"**(SceneArrange.cpp:172 注释"阶段8替换")。
   B 的真 AI 已就绪(`AIPlayer::decideOrderStyled`,胜率表 assets/ai/winrate.bin 已生成),
   **只需把随机 shuffle 换成一行 `AIPlayer::decideOrderStyled(...)` 调用**。建议:B 或 D 顺手替换(约5分钟)。
4. **按钮贴图素材已入库但未显示**:`assets/ui/buttons/btn_*.png`(亮金四态)AssetManager 已加载,
   但 Button 控件画的是纯色矩形。若要用贴图按钮需改 Button::draw(C 后续打磨期处理);
   不处理则删素材,二者选一。

### 🟢 低/记录
5. 牌素材尺寸已从 338×488 统一为 200×280(与 CardSprite 基准一致,修复组牌布局错位)。
6. 素材清单(assets/README.md)与代码 AssetManager 全部对齐,零"加载失败"日志。

## 四、结论与建议

- **A+B+C+D 代码库当前可编译、可启动、组牌闭环可用**(发牌→点牌分三道→交牌),
  但 **"比牌→结算"是空壳**,完整一局尚未跑通——符合两周计划进度(C 的第6/7天任务还没到期)。
- **建议本周节奏**:
  1. C 第6天交付 SceneBattle(四家牌面对照+逐道翻牌),D 支援素材(胜负标记/翻牌音效已备好);
  2. B 或 D 顺手把组牌界面的假 AI 替换成真 AIPlayer(问题3);
  3. C 第7天交付 SceneResult,达成"周度集成:1真人+3AI 带界面完整一局"。
- **下个检查点**:第7天周度集成后,由 D 全流程实测并发布 build。
